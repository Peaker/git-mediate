set -eu

DIR="$( dirname "$0" )"

export git_mediate=../dist/build/git-mediate/git-mediate

echo Spaces test
${DIR}/spaces/test

echo Untabify test
${DIR}/untabify/test

echo Config test
${DIR}/config/test


echo Tests successful
